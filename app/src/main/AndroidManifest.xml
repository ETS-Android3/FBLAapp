<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nickli.scheduleapp">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScheduleApp"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true">
        <meta-data
            android:name="com.google.firebase.ml.vision.DEPENDENCIES"
            android:value="ocr"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".SignUpActivity"></activity>
        <activity android:name=".schedule.ScheduleActivity"></activity>
        <activity android:name=".schedule.ScheduleAdd"></activity>
        <activity android:name=".lunch.LunchActivity"></activity>
        <activity android:name=".emailStaff.EmailActivity"></activity>
        <activity android:name=".emailStaff.EmailAdd"></activity>
        <activity android:name=".extraActivities.ClubActivity"></activity>
        <activity android:name=".extraActivities.ClubAdd"></activity>
        <activity android:name=".CalendarActivity"></activity>
        <activity android:name=".schedule.ScheduleRemind"></activity>
    </application>

</manifest>